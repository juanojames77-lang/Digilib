<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Search Results</title>
<style>
  body { font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; padding:40px; background:#ecf0f1; }
  h1 { color:#3f51b5; margin-bottom:20px; }
  ul { list-style:none; padding:0; }
  li { background:white; margin-bottom:10px; padding:15px; border-radius:8px; box-shadow:0 2px 6px rgba(0,0,0,0.1); transition:0.2s; }
  li:hover { transform:scale(1.02); }
  a { text-decoration:none; color:#3f51b5; font-weight:500; }
  a:hover { color:#2c387e; }
</style>
</head>
<body>
  <h1>Search Results for "<%= query %>"</h1>
  <% if(results.length === 0){ %>
    <p>No theses found.</p>
  <% } else { %>
    <ul>
      <% results.forEach(r => { %>
        <li><a href="/view/<%= r.id %>"><%= r.title %></a> - <%= r.course %> <% if(r.is_private){ %> 🔒 <% } %></li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
